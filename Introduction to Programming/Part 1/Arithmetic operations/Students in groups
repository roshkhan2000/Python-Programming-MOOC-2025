# Write your solution here
students = float(input("How many students on the course?"))
size = float(input("Desired group size?"))
import math
no_of_groups = math.ceil(students/size)

print(f"Number of groups formed: {no_of_groups}")
